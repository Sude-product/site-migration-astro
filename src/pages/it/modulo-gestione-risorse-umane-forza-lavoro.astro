---
// Aşama 3 (kısım 2) — İK Hub sayfası (KARAR 3 gereği ayrıca ele alındı). Bkz. CLAUDE.md.
import HubPage from '../../components/HubPage.astro';
import { getHubContent, getHubLocaleUrls, getHubPageTitle } from '../../data/hubContent';

const trSlug = 'insan-kaynaklari-isgucu-yonetimi';
const content = getHubContent(trSlug, 'it')!;
const pageTitle = getHubPageTitle(trSlug, 'it');
const localeUrls = getHubLocaleUrls(trSlug);
---

<HubPage content={content} pageTitle={pageTitle} localeUrls={localeUrls} hreflangLocales={['tr', 'en', 'it']} />
