---
export const prerender = true;
// 2026-08-05: kapsamlı URL denetimiyle bulunan, önceki turlarda kaçırılmış
// bir sayfa (ana KVKK Aydınlatma Metni'nden ayrı bir hukuki doküman). Bkz.
// CLAUDE.md.
import LegalPage from '../../components/LegalPage.astro';
import { getKvkProtocolContent, getKvkProtocolLocaleUrls, getKvkProtocolModifiedDate } from '../../data/miscPagesContent';

const content = getKvkProtocolContent('en')!;
const localeUrls = getKvkProtocolLocaleUrls();
const modified = getKvkProtocolModifiedDate('en');
const description =
  "Review the terms of idenfit's Protocol on the Protection of Personal Data, including obligations and data processing principles.";
---

<LegalPage content={content} localeUrls={localeUrls} description={description} hreflangLocales={['tr', 'en']} modified={modified} />
