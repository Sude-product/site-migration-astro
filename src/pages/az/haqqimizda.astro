---
export const prerender = true;
import AboutPage from '../../components/AboutPage.astro';
import { getAboutContent, getAboutLocaleUrls, getAboutTitle } from '../../data/miscPagesContent';

const content = getAboutContent('az')!;
const pageTitle = getAboutTitle('az');
const localeUrls = getAboutLocaleUrls();
---

<AboutPage content={content} pageTitle={pageTitle} localeUrls={localeUrls} />
